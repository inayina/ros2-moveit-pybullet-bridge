[develop]
script_dir=$base/lib/pybullet_bridge
[install]
install_scripts=$base/lib/pybullet_bridge

[tool:pytest]
testpaths = test
python_files = test_*.py
addopts = -ra
markers =
    launch_test: ROS 2 launch integration test (slower)
